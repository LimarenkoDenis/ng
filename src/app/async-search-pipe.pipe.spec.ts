import { AsyncSearchPipePipe } from './async-search-pipe.pipe';

describe('AsyncSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AsyncSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
